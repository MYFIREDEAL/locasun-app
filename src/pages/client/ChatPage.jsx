import React from 'react';
import ChatConversationsList from '@/components/client/ChatConversationsList';

/**
 * Page Chat — Liste des conversations (WhatsApp-style)
 * Route: /dashboard/chat
 */
const ChatPage = () => {
  return <ChatConversationsList />;
};

export default ChatPage;
